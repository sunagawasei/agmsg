# Runtime data (created at install destination, not in repo)
db/
teams/
.DS_Store
.codex/
.claude/
.cursor/
.opencode/
.playwright-mcp/

# Nix devShell: direnv が allow 時に生成するローカルcache。
# flake.nix/.envrc/flake.lock はコミット済みで、生成物だけ除外する。
.direnv/
__pycache__/

/.omc/
/.omx/
/backlog/

# Local secrets (notarization credentials, updater signing key, etc.) — never commit
.env
.env.*
/.secrets/
