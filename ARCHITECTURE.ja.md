# アーキテクチャ

*[English](ARCHITECTURE.md)*

このドキュメントは agmsg の内部構造を説明する — コードやインターフェース仕様を読む前にコントリビューターが持っておくべきメンタルモデルである。「何を」実装するかについては [`docs/spec/`](docs/spec/) を参照。「なぜ」その設計判断がなされたかについては [`docs/adr/`](docs/adr/) を参照。

## ゴール

Claude Code、Codex、Gemini CLI、Antigravity、そして将来登場するエージェントランタイムの、あらゆる組み合わせの間で動作するクロスエージェントメッセージングのプリミティブ — デーモンなし、ネットワークなし、共有クラウドなし。メッセージは単一マシン上のローカルファイルを通じて移動し、受信側はホストランタイムが提供するフックやストリーミング機構を通じて通知を受ける。

デフォルトのインストールは **bash + sqlite3 のみ**で動作しなければならない。それを超える機能はすべてオプトインであり、ユーザーが同意した場合に限り追加の依存関係を要求してよい。

## 3軸ドライバーモデル

agmsg は3つの直交する軸を中心に構築されており、各軸には常にちょうど1つの**ドライバー (driver)** がアクティブになる。ドライバーとは、固定されたプロトコルの背後にある差し替え可能な実装である。

| 軸 | 抽象化する対象 | バンドルされているドライバー |
|---|---|---|
| **storage（ストレージ）** | メッセージとチームの状態がどこに存在し、どのようにクエリされるか | `sqlite`（デフォルト）、`jsonl-duckdb` |
| **agent（エージェント）** | ランタイムごとの差異（フック形式、設定ファイルの場所、monitor ツールの有無） | `claude-code`、`codex`、`gemini`、`antigravity`、`copilot` |
| **delivery（配信）** | 受信者にメッセージの到着をどう通知するか | `monitor`、`turn`、`both`、`off` |

3つの軸は独立している。どの storage ドライバーも、どの agent ドライバー・どの delivery モードとも組み合わせ可能である。これらは共通の discovery/config/dependency-check プロトコル（仕様参照）を共有しつつ、軸ごとに固有の操作を公開する。

## ドライバー (driver) と プラグイン (plugin)

- **driver（ドライバー）** はアーキテクチャ上の単位である。バンドルされた実装もユーザーがインストールしたものも、どちらもドライバーである。
- **plugin（プラグイン）** は配布方法上の区別である。agmsg コアの外側から提供され、ユーザーが管理するディレクトリに配置されるドライバーを指す。

バンドルされたドライバーは `scripts/drivers/<axis>/` 配下に存在する。`scripts/` の中では、トップレベルの `.sh` ファイルは直接実行可能なコマンドであり、サブディレクトリは実装コードをカテゴリ別にグループ化する（軸ドライバー用の `drivers/`、共有ヘルパー用の `lib/`）。

プラグインパス（`~/.agents/agmsg/plugins/<axis>/<name>/`）は将来の拡張のために予約されており、v1 では未実装である。具体的な外部ドライバーが必要になるまでは、すべてのドライバーがバンドル形式で提供される。いずれも同じプロトコルを通じて発見・ロードされる。

agmsg では *backend*、*extension*、*provider*、*adapter* という用語は使用しない。ユーザー向けドキュメントや CLI において `storage` は「storage driver（ストレージドライバー）」の同義語として書かれる。

## 依存関係の管理

ドライバーは外部ツールを必要とする場合がある（例: `jsonl-duckdb` storage ドライバーは `$PATH` 上に `duckdb` を必要とする）。agmsg はそれらのツールを自分自身ではインストールしない。代わりに次の流れをとる。

1. ドライバーが有効化されたとき、あるいはその `check` サブコマンドが呼び出されたとき、ドライバーは自身の実行環境を検査する。
2. 依存関係が不足している場合、ドライバーは何をどうインストールすべきかを記述した**機械可読な `AGMSG-DIRECTIVE`** 行を stdout に出力する。
3. ホストエージェント（Claude Code、Codex など）はそのディレクティブを読み取り、ユーザーの同意を得た上で、自身のツールを使ってインストールコマンドを実行する。
4. 次に agmsg が呼び出されたときには依存関係チェックが通過し、ドライバーが有効化される。

これにより agmsg 自体は最小限かつプラットフォームに依存しない状態を保てる。ホストエージェントはすでにファイルアクセスやシェルアクセスを信頼されているため、依存関係のインストールはホストエージェントの責務とするのが自然である。

同じ `AGMSG-DIRECTIVE` の仕組みは、他のホストエージェント連携アクション（例: delivery モードが `monitor` に設定されたときに Claude Code に Monitor ツールを呼び出すよう伝える、など）にも使われる。

## 失敗時の挙動 (Failure semantics)

storage ドライバーの切り替えは、agmsg が行う中で最も影響の大きい操作である。このプロトコルは、システムを復旧可能な状態に保つことを優先する。

- `agmsg storage switch` 実行中の依存関係チェックの失敗は、アクティブなドライバーを変更**しない**。ユーザーにはディレクティブが提示され、フォールバックが与えられる。
- `convert` は常にまずステージング用ストアに書き込み、それを検証してから、アトミックな設定切り替えを行う。最終ステップに至るまで、以前のドライバーはアクティブかつ無傷のまま維持される。
- ステータスコードは構造化されている（`ok`、`missing_deps`、`incompatible_core`、`corrupt_state`、`runtime_error`）ため、ホストエージェントはすべての非ゼロ終了コードを一律に扱うのではなく、状況に応じて適切に反応できる。

## ストレージの構造

storage ドライバーは、agmsg の他の部分に対して CRUD ライクな操作（メッセージの挿入、エージェントの未読一覧取得、既読化、履歴のクエリ）を公開する。内部的には、バンドルされたドライバーは状態を**追記専用のイベントログ (append-only event log)**（`message_sent`、`message_read` など）として表現する。可変なステータス — 最も重要なのは既読フラグ — は、インプレース更新によってではなく、イベントストリームに対する projection（射影）によって導出される。

sqlite ドライバーは、インライン `read` カラムを持つレガシーな `messages` テーブルに対する後方互換の読み取りパスを維持しており、既存のインストールはマイグレーションなしで動作し続ける。新しい書き込みはイベントログへ行われ、読み取りは両方のソースを union する。

メッセージ識別子は **UUIDv7** 文字列である。インターフェースはこれを不透明な値として扱うため、整数の自動採番 ID を持つ既存の sqlite データベースも引き続き読み取り可能である。

## 用語集

| 用語 | 意味 |
|---|---|
| **driver（ドライバー）** | ある軸上の固定プロトコルの、差し替え可能な実装 |
| **plugin（プラグイン）** | agmsg コアの外側で配布されるドライバー |
| **bundled driver（バンドルドライバー）** | agmsg コアに同梱されて出荷されるドライバー |
| **storage（ストレージ）** | ユーザー向けの文脈における storage 軸、またはそのアクティブなドライバー |
| **AGMSG-DIRECTIVE** | ホストエージェントに特定のアクションを取るよう指示するために stdout に出力される JSON 行 |
| **host agent（ホストエージェント）** | agmsg スクリプトを呼び出すランタイム（Claude Code、Codex、Gemini CLI、Antigravity、…） |
| **event log（イベントログ）** | バンドルされた storage ドライバーがクエリの射影元とする、メッセージのライフサイクルイベントの追記専用記録 |

## 関連ドキュメント

- [`docs/spec/driver-interface.md`](docs/spec/driver-interface.md) — ドライバーが満たすべき正式な契約
- [`docs/adr/`](docs/adr/) — 設計判断の歴史的記録
- [`CONTRIBUTING.md`](CONTRIBUTING.md) — 変更を提案する方法
